#!/bin/bash
set -euo pipefail

PATTERN='^[a-zA-Z0-9._%-]+$'

for arg in "$@"; do
    if [[ ! "$arg" =~ $PATTERN ]]; then
        echo "Rejected invalid argument: $arg" >&2
        exit 1
    fi
done

exec /usr/bin/tc "$@"
